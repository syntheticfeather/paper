
const canvas = document.getElementById('canvas1'); // 获取 canvas 元素
const ctx = canvas.getContext('2d'); // 获取 2D 绘图上下文

// 填充矩形
ctx.fillStyle = '#4a86e8'; // 设置填充颜色
ctx.fillRect(3, 3, 50, 50); // x, y, width, height
